# Example .env configuration for Linux
# Copy this to .env and fill in your actual credentials

# ============================================================================
# Admin Credentials (for Ezaango login)
# ============================================================================
ADMIN_USERNAME_HAHS_VIC3495=your_email@example.com
ADMIN_PASSWORD_HAHS_VIC3495=your_password

# ============================================================================
# TOTP Secret (for 2FA code generation)
# ============================================================================
TOTP_SECRET_HAHS_VIC3495=YOUR_TOTP_SECRET_HERE

# ============================================================================
# SMTP Configuration (Gmail/Outlook for email notifications)
# ============================================================================
SENDER_EMAIL=your_email@gmail.com
EMAIL_APP_PASSWORD=your_app_password_here
COLLECTOR_EMAIL=collector@example.com

# Optional: Override SMTP settings (defaults to Gmail)
SMTP_SERVER=smtp.gmail.com
SMTP_PORT=465

# ============================================================================
# Audio Device Configuration (for Voice Assistant - LINUX)
# ============================================================================
# IMPORTANT: For Linux, use the PulseAudio virtual sink created by setup_pulseaudio.sh
# Run: bash scripts/setup_pulseaudio.sh before starting the voice assistant
TTS_OUTPUT_DEVICE=virtual_speaker
